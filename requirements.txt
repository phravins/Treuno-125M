# ============================================================
# Treuno 125M — Full Requirements
# ============================================================

# ── Core ML / Training ───────────────────────────────────────
torch>=2.2.0                    # PyTorch 2.2 with FlashAttention-2 support
torchvision
torchaudio
transformers>=4.38.0            # HuggingFace Transformers
datasets>=2.18.0                # HuggingFace datasets (HumanEval, MBPP)
tokenizers>=0.15.0
accelerate>=0.27.0              # Multi-GPU training utilities
deepspeed>=0.14.0               # DeepSpeed ZeRO-2 distributed training
flash-attn>=2.5.0               # FlashAttention-2 (requires CUDA)
peft>=0.9.0                     # LoRA / PEFT adapters
trl>=0.8.0                      # SFTTrainer, DPOTrainer

# ── Quantization / Inference ─────────────────────────────────
auto-gptq>=0.7.0                # AutoGPTQ int4 quantization
optimum>=1.17.0                 # HuggingFace Optimum for GPTQ export
vllm>=0.3.0                     # vLLM with PagedAttention serving

# ── Experiment tracking ───────────────────────────────────────
mlflow>=2.10.0                  # Experiment tracking

# ── Retrieval / RAG ──────────────────────────────────────────
sentence-transformers>=2.6.0    # all-MiniLM-L6-v2 embeddings (384-dim)
faiss-gpu>=1.7.4                # FAISS IVF-PQ vector search (GPU)
faiss-cpu>=1.7.4                # FAISS CPU fallback
rank-bm25>=0.2.2                # BM25 keyword search

# ── Reranking ─────────────────────────────────────────────────
# cross-encoder/ms-marco-MiniLM-L-6-v2 via sentence-transformers above

# ── Web retrieval ─────────────────────────────────────────────
requests>=2.31.0
duckduckgo-search>=5.0.0        # DuckDuckGo fallback (no API key)
httpx>=0.27.0                   # Async HTTP client
beautifulsoup4>=4.12.0          # HTML parsing for full page fetch
lxml>=5.1.0

# ── API / Serving ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# ── Caching ───────────────────────────────────────────────────
redis>=5.0.0                    # Redis 7 client (AG-Cache)

# ── Streaming / Orchestration ─────────────────────────────────
kafka-python>=2.0.2             # Apache Kafka consumer
apache-airflow>=2.8.0           # Airflow DAG orchestration

# ── Deduplication ─────────────────────────────────────────────
datasketch>=1.6.0               # MinHash LSH deduplication

# ── Storage / Data ────────────────────────────────────────────
pyarrow>=15.0.0                 # Parquet read/write
pandas>=2.2.0                   # DataFrame operations
s3fs>=2024.2.0                  # S3 filesystem for Parquet on AWS
gcsfs>=2024.2.0                 # GCS filesystem for Parquet on GCP
numpy>=1.26.0

# ── Docker / Sandbox ──────────────────────────────────────────
docker>=7.0.0                   # Docker Python SDK (AG-Execute)

# ── Monitoring ────────────────────────────────────────────────
prometheus-client>=0.20.0       # Prometheus metrics

# ── Rich CLI ──────────────────────────────────────────────────
rich>=13.7.0                    # CLI output formatting

# ── Evaluation ────────────────────────────────────────────────
# HumanEval and MBPP via datasets above
# MultiPL-E: loaded from HuggingFace datasets nuprl/MultiPL-E

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0            # .env file loading
tqdm>=4.66.0                    # Progress bars
tenacity>=8.2.0                 # Retry logic
loguru>=0.7.0                   # Structured logging (optional)
